'use server';

// Categories, Locations, and Property Status server actions will be defined here in Phase 3
export async function createMetadata(type, data) {
  return { success: false, error: 'Not implemented' };
}

export async function updateMetadata(type, id, data) {
  return { success: false, error: 'Not implemented' };
}

export async function deleteMetadata(type, id) {
  return { success: false, error: 'Not implemented' };
}
